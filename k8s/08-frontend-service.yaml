apiVersion: v1
kind: Service
metadata:
  name: frontend
  namespace: lab5-chat
  labels:
    app: chat-app
    component: frontend
spec:
  selector:
    app: chat-app
    component: frontend
  type: NodePort
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30080
