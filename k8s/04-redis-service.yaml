apiVersion: v1
kind: Service
metadata:
  name: redis
  namespace: lab5-chat
  labels:
    app: chat-app
    component: redis
spec:
  selector:
    app: chat-app
    component: redis
  ports:
    - port: 6379
      targetPort: 6379
  type: ClusterIP
